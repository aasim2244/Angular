import { OrientationDirective } from './orientation.directive';

describe('OrientationDirective', () => {
  it('should create an instance', () => {
    const directive = new OrientationDirective();
    expect(directive).toBeTruthy();
  });
});
