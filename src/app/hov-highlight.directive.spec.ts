import { HovHighlightDirective } from './hov-highlight.directive';

describe('HovHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HovHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
